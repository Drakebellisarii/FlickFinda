services:
  - type: web
    name: flickfinda
    env: python
    buildCommand: pip install -r requirements.txt && cd frontend && npm install && npm run build
    startCommand: cd src/movie_app && gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: OMDB_API_KEY
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: YOUTUBE_API_KEY
        sync: false
